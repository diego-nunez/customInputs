var input = new customizeInputs({
    exclude: 'noSelected',
    general: 'customizeInput',
    floatLabel: {
        'select': true,
        'general': true,
        'excludeClasses': ['noFloatLabel']
    }
})

input.customInputs();